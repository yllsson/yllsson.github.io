const titleImgContainer = document.getElementById('titleImages');

document.addEventListener('DOMContentLoaded', () => {
  titleImgContainer.classList.add('loaded');
  console.log('loaded', titleImgContainer);
});
